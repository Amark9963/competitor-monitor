# web

Next.js dashboard for the competitor monitor. See the [root README](../README.md) for setup.

```bash
npm install
npm run dev   # expects the API on http://127.0.0.1:8000 (override with API_URL)
```
